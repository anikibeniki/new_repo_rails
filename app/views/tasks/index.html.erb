<ul>
  <% @tasks.each do |task| %>
      <li> <% if task.completed%>
      <p>x</p>
      <% else %>
      <p>_</p>
      <% end %>
       <%=link_to task.title, task_path(task) %></li>
  <% end %>
</ul>
